const baseURL = `https://which-technical-exercise.herokuapp.com/api`;
const directionsURL = `directions`;
const locationURL = `location`;
const allowedDirections = ['right', 'left', 'forward'];

module.exports = {baseURL, directionsURL, locationURL, allowedDirections};
